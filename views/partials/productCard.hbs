

<div class="card ">
  <div class="card-header">
    {{inHTMLData title}}
  </div>
  <div class="card-block">
    <h4 class="card-title">{{inHTMLData name}}</h4>
    <p class="card-text">{{inHTMLData description}}</p>
    <a href="{{inDoubleQuotedAttr link}}" class="btn btn-primary">Preview</a>
    <a href="{{inDoubleQuotedAttr link}}" class="btn btn-info">${{inHTMLData price}}</a>
  </div>
</div>
